services:
  - type: web
    name: calgary-dashboard-backend
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: cd backend && python app.py
    envVars:
      - key: HF_API_KEY
        sync: false